Checking for module docstring:
         >>> u = __import__("1-my_list").__doc__
         >>> len(u) > 1
         True

Checking for class docstring:
         >>> u = __import__("1-my_list").MyList.__doc__
         >>> len(u) > 1
         True

Checking for method docstring:
	  >>> u = __import__("1-my_list").MyList.print_sorted.__doc__
	  >>> len(u) > 1
	  True

Checking that MyList inherits from list:
	  >>> issubclass(MyList, list)
	  True

Checking for empty list:
	  >>> u = MyList()
	  >>> u.print_sorted()
	  []

Checking that appendng works:
	  >>> u.append(1)
	  >>> u.append(2)
	  >>> u.append(3)
	  >>> u.append(4)
	  >>> print(u)
	  [1, 2, 3, 4]

Checking for list already in correct order:
	  >>> u.print_sorted()
	  [1, 2, 3, 4]

Checking for reversed order list:
	  >>> u = MyList()
	  >>> u.append(4)
	  >>> u.append(3)
	  >>> u.append(2)
	  >>> u.append(1)
	  >>> print(u)
	  [4, 3, 2, 1]
	  >>> u.print_sorted()
	  [1, 2, 3, 4]
	  >>> print(u)
	  [4, 3, 2, 1]

Checking for one negative number:
	  >>> u.append(-1)
	  >>> u.append(5)
	  >>> print(u)
	  [4, 3, 2, 1, -1, 5]
	  >>> u.print_sorted()
	  [-1, 1, 2, 3, 4, 5]
	  >>> print(u)
	  [4, 3, 2, 1, -1, 5]

Checking for all negative numbers:
	  >>> u = MyList()
	  >>> u.append(-10)
	  >>> u.append(-1)
	  >>> u.append(-7)
	  >>> u.append(-2)
	  >>> u.append(-8)
	  >>> print(u)
	  [-10, -1, -7, -2, -8]
	  >>> u.print_sorted()
	  [-10, -8, -7, -2, -1]
	  >>> print(u)
          [-10, -1, -7, -2, -8]

Checking for too many arguments:
	  >>> u.print_sorted(1)
	  Traceback (most recent call last):
	  ...
	  TypeError: print_sorted() takes 1 positional argument but 2 were given
